group by 사용시 주의할 점
원칙) group by에 명시한 컬럼만이 select 절의 대상이 될 수 있다.
예외) 집계함수는 group by에 명시하지 않아도 select 절에 올 수 있다.
	왜?? 이미 그룹화된 결과이므로....

distinct는 중복제외일뿐 그룹화한 것이 아니다(통계와 상관이 없다...)

-------------------------------------------------------------------------------------------------
(pseudo)유사, 가짜, 가상 클래스, 엘리먼트
selector: id, class, name, tag 명 상태나 다른 요소와의 관계에서의 위치 등으로도 지적할 수 있음
	selector 확장

-------------------------------------------------------------------------------------------------
자바스크립트 개요
1.틀린 것
(1), (3)
Dynamic HTML

2.틀린 것
(3), (4), (5)

3.틀린 것

4.틀린 것
(1)
-------------------------------------------------------------------------------------------------
할 것
변수, 자료형 복습(읽어보기), 자바스크립트로 갤러리 만들기, 페이딩 효과
폴더 정리, 깃텁 해결....
-------------------------------------------------------------------------------------------------
cpu는 연산능력이있다
메모리는 저장소이다
메모리와 하드디스크의 차이점: 속도(메모리가 훨씬 빠르다)
명령을 메모리에다가 한다.
우리가 저장하는 데이터(html, 게임프로그램 등)들은 하드디스크에 저장된다.
단, 그 프로그램들을 더블클릭(실행)하면 메모리로 스물스물 들어온다(로딩).











